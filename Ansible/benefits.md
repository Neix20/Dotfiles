
# Benefits

- Outpus results in a way that integrates with rest of reporting
- Avoid unnecessary actions updates
- Advice about change status
- Return Before and After Diff Changes when requested
- Report Missing Requirements in user friendly way
- Provide Logging and Debugging on demand
- Provide Readablxe Documentation, Required parameters, types, examples, snippets
- Parameter Validation, Fallback to defaults or even environment Vars
- Privacy Protection (No Log)

*Remember*

Ansible is used for configuration management. It is used by business, so it needs to be easy to manage, easy to document and easy to debug.
